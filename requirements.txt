anthropic>=0.18.0
selenium>=4.15.0
pyautogui>=0.9.54
requests>=2.31.0
pillow>=10.0.0
webdriver-manager>=4.0.0
edge-tts>=6.1.0
transformers>=4.30.0
torch>=2.0.0
sentencepiece>=0.1.99

# Optional - pydub doesn't work on Python 3.13+ (audioop removed)
# The script will use ffprobe instead if pydub is not available
# pydub>=0.25.1
